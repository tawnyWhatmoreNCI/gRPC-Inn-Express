<!--nav bar-->
<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand ms-2" href="/">gRPC Inn</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <div class="ms-auto me-2">
      <button class="btn btn-outline-primary my-2 my-sm-0" id="toggleUser">Switch to 
        <% if (isAdmin) { %>
          User 
        <% } else { %>
          Admin
        <% } %>
        </button>
    </div>

  </div>
</nav>
<script>
//post to /toggleUserState to switch user state
document.getElementById('toggleUser').addEventListener('click', async () => {
  try {
    const response = await fetch('/toggleUserState', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json'
      }
    });
    if (!response.ok) {
      throw new Error('Network response was not ok');
    }
    window.location.reload();
  } catch (error) {
    console.error('Error:', error);
  }
});
</script>

